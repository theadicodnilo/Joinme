{
  "name": "join-me",
  "version": "1.0.0",
  "description": "Join Me Chat Application",
  "scripts": {
    "start": "netlify dev",
    "build": "mkdir -p public && cp *.html public/ && netlify build",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "serverless-http": "^3.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
